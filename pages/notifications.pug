.content
  header(load-order="primary")
    .types
      .type Unread
      .type(selected) All notifications

  .inner-content(load-order="secondary")

script.
  lazyload('/pages/notifications.js', document.currentScript.closest('soci-route'))