header(load-order="primary")
  .hero
    soci-user(size="x-large" self)
    .subtitle Conjurer for Deep Design and Theoretical Ideas
  .money
    .title $4,269
    .label current earnings
    .up  +2.5%

  .subscribers
    .title 1.8k
    .label subscribers
    .up  +0.4%

.content(load-order="secondary")
  .col
    .col-header
      soci-icon(glyph="tags")
      | My Posts
    soci-post-list.my-posts(data="/posts")
  .col
    .col-header
      soci-icon(glyph="comments")
      | Comments
    soci-post-list.my-comments(data="/posts")

script(src="/pages/user.js")