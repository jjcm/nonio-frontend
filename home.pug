soci-router
  soci-sidebar
  #pages
    soci-route#tags(pattern="^#" title="tags")
      include pages/tags.pug
    soci-route#home(pattern="^$" title="home")
      include pages/home.pug
    soci-route#user(pattern="^user$" title="user")
      include pages/user.pug